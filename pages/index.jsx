import { Layout } from "components/Layout";
import { Auth } from "components/Auth";

const Home = () => {
  return (
    <Layout title="Login">
      <Auth />
    </Layout>
  );
};

export default Home;
